@model IEnumerable<ArticleSystem.Web.Models.Home.ArticleHomeViewModel>

@*<h1>Articles</h1>
    <hr />

    <div class="row">
        @foreach (var item in Model)
        {

            <div class="col-md-4">
                <h2>@item.Name</h2>
                <p>
                    Descripton:
                    @item.Description
                </p>
                <p></p>
                <p><a class="btn btn-default" href="@Url.Action("Details", "Article", new {id=  item.Id })">Learn more &raquo;</a></p>
            </div>
        }
    </div>*@
    
                      <div class="jumbotron">
                          <h1>Article</h1>
                          <p class="lead">These are the lates items added</p>
                      </div>

                      <div class="row" id="thumbnails">
                          @foreach (var product in Model)
                          {
                              @Html.Partial("_ThumbnailPartial", product);
                          }
                      </div>
